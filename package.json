{
  "name": "painel-gaq",
  "private": true,
  "version": "6.1.0",
  "scripts": {
    "build": "node --use-system-ca scripts/build.mjs",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
